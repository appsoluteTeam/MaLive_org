<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abbsolute.ma_livu">
    <!-- 인터넷 허가 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Login.GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login.Signup3Activity"></activity>
        <activity android:name=".Login.Signup2Activity" />
        <activity android:name=".Login.FindpwdActivity" />
        <activity android:name=".Login.Login2Activity" /> <!-- GlobalApplication 객체를 공유하기 위함 -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />

        <activity android:name=".Community.CommuWriteActivity" />
        <activity android:name=".Community.CommunityActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Login.SignupActivity" />
        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <activity android:name=".VisitorBoard.CommentWriteActivity"></activity>
        <activity android:name=".VisitorBoard.GuestBookActivity" />

        <service
            android:name=".VisitorBoard.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>